#!/bin/bash
set -e

CUID="$(stat -c %u ~)"
CGID="$(stat -c %g ~)"

cp chembience.env .env

sed -i -e "s/@CHEMBIENCE_UID/${CUID}/g" .env
sed -i -e "s/@CHEMBIENCE_GID/${CGID}/g" .env

sed -i -e "s/@CHEMBIENCE_GID/`pwd`/g" .env


